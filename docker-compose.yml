version: "3.9"

services:
  frontend:
    build:
      context: ./maatiafrontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8080

  backend:
    build:
      context: ./maatia
    ports:
      - "8080:8080"
